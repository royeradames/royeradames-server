{
  "extends": "./tsconfig.json",
  // don't let ts compile omconfig or migrations because they are already in JS
  "exclude": ["ormconfig.js","migrations","node_modules", "test", "dist", "**/*spec.ts"]
}
